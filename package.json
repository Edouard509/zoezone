{
  "name": "zoezone",
  "version": "1.0.0",
  "private": true,
  "description": "ZOEZONE storefront — static frontend + Netlify Functions backend (Netlify Database / Neon Postgres).",
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/database": "^1.0.0",
    "@netlify/blobs": "^8.1.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
